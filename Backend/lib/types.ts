export type UserType = {
    id?: number;
    firstName: string;
    lastName: string;
    email: string;
    password: string;
};

export type ProjectType = {
    id?: number;
    name: string;
    budget: number;
    deadline: string;
    description: string;
};
